import React, { Component } from 'react';
import { View } from 'react-native';

export default class PodcastCarousel extends Component {
    state = {}
    render() {
        return (
            
        );
    }
}